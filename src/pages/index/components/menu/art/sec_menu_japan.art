<div class="menu_inn">
    <dl>
        {{each japan}}
        <dt>{{$value.title}}</dt>
        <dd>
            {{each $value.cities}}
            <a href="">{{$value.text}}</a>
            {{/each}}
        </dd>
        {{/each}}
    </dl>
</div>